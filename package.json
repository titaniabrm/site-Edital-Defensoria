{
  "name": "edital-defensoria",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --use-system-ca server.js",
    "dev": "node --watch --use-system-ca server.js",
    "check": "node --check server.js && node --check app.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pdfkit": "^0.15.0",
    "pino": "^9.4.0"
  },
  "devDependencies": {
    "vitest": "^2.1.1"
  }
}
